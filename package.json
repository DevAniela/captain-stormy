{
  "name": "will-it-rain",
  "version": "1.0.0",
  "description": "A simple website that uses the Open-Meteo API to tell the user if it will rain tomorrow, helping them decide if they should take an umbrella when leaving the house.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "DevAniela",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "ejs": "^3.1.10",
    "express": "^4.19.2"
  }
}
